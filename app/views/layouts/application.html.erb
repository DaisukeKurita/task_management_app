<!DOCTYPE html>
<html>
  <head>
    <title>TaskManagementApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <% if logged_in? %>
        <li><% link_to "Profile", user_path(current_user.id) %></li>
        <li><% link_to "Logout", section_path(current_user.id), method: :delete %></li>
      <% else %>
      <ul>
        <li><%= link_to "Sign up", new_user_path %></li>
        <li><%= link_to "Login", new_session_path %></li>
        <li><%= link_to t('view.Create a new task'), new_task_path %></li>
        <li><%= link_to t('view.Return to task list screen'), tasks_path %></li>
      </ul>
      <% end %>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
    </header>
      <main>
        <%= yield %>
      </main>
    <footer>
      <p>タスク管理で仕事の生産性がアップするよ</p>
    </footer>
  </body>
</html>
